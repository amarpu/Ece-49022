name: aquarium_controller_app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # For MQTT communication
  mqtt_client: ^10.1.0 
  
  # For state management
  provider: ^6.1.1

  # Desktop/local notifications (Windows, macOS, Linux)
  local_notifier: ^0.1.6

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/app_logo.png
